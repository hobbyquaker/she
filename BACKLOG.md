# Backlog

Items that are intentionally deferred. Pick up when the time is right.

## AI Assistant

- **Conversation persistence across sessions** — currently chat history lives in `localStorage` and is lost on page reload. Store conversations server-side in ~/.she/ai/<sessionid> so history survives across browsers and devices.

## Git Integration

*(nothing yet)*

## General

 - **per-script resource limits** - detect callbacks that take too long, log a warning

 ## sheDB

 - add **description property for views** and text input field to db views ui