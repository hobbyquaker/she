# M3 — Raw `mosquitto.conf` editor ✅ implemented
*Archived roadmap item — MQTT, Matter & Broker. Open items: [../../ROADMAP.md](../../ROADMAP.md) · Index: [README.md](README.md)*

**✅ Implemented** in commit 89bd9dc (*"Monaco editor for mosquitto.conf (Advanced tab) and Auth Files tab"*) — the item was simply never archived; found still open in ROADMAP.md on 2026-08-28 and closed after verifying the shipped UI against the spec.

[web/src/pages/broker/Advanced.svelte](../../web/src/pages/broker/Advanced.svelte), mounted as the **Advanced** sub-tab of the Broker page in [web/src/pages/Security.svelte](../../web/src/pages/Security.svelte): the full `mosquitto.conf` in a Monaco editor with `language: 'ini'`, *Save* via `putBrokerConfRaw(content, checksum)` with the checksum-based external-modify detection (409 → "modified externally, reload first"), *Apply & Reload*, and the backup list with one-click restore (`POST /she/broker/config/restore`) — everything the item asked for.

## Original item

The `PUT /she/broker/config/raw` endpoint and the `putBrokerConfRaw()` API helper already exist; only the frontend tab is missing. Add an **Advanced** sub-tab to the Broker page that renders the full `mosquitto.conf` text in a Monaco editor (`language: 'ini'`). On save, call `putBrokerConfRaw(content, checksum)` — external-modify detection via checksum works the same way as the structured editor. The tab should also surface the backup list with a one-click restore (calls `POST /she/broker/config/restore`). This gives power users a full escape hatch beyond what the structured Listeners / Global Config tabs expose.
